# Task 1. Исследование моделей и инфраструктуры

## Сравнение LLM моделей

Для сравнения не берем YandexGPT, т.к. он больше ориентирован на русскоязычный контекст и российскую экосистему. Для иностранной компании с учетом санкций этот вариант не рассматриваем.

| Критерий | Локальные Hugging Face (Llama 3.1 70B / Mixtral / Qwen3) | Облачные OpenAI (GPT-4o / GPT-4o mini / o3-mini) |
| --- | --- | --- |
| **Качество ответов** | Среднее - хорошее | Отличное, лучший reasoning и точность |
| **Скорость** | Зависит от инфраструктуры: без GPU — медленно (до нескольких секунд на токен) | Очень быстро |
| **Стоимость** | Капитальные затраты: $1500–6000 (GPU) + сопровождение (MLops) | $0.15–$15 за 1M токенов (mini дешево, o3 дорого) → 1000 запросов/день ≈ $10–150/мес |
| **Развёртывание** | Сложнее: Docker, GPU, GGUF/vLLM/Ollama | 15–30 минут (API + SDK), |

**Вывод**:  
Учитывая работу с конфиденциальной документацией (ABB, Wärtsilä, SOC 2), **локальная LLM предпочтительна**.  
Возможен гибридный подход при наличии enterprise-договора с защитой данных.

## Модели эмбеддингов

| Критерий | Локальные Sentence-Transformers | Облачные OpenAI Embeddings |
| --- | --- | --- |
| **Скорость создания индекса** | Зависит от hardware (CPU/GPU). Можно масштабировать в зависимости от потребностей. | Высокая, но ограничена лимитам (~3000 req/min) |
| **Качество поиска** | Высокое для английского и технического текста, уступает в сложном reasoning. | Лучшее в отрасли, но, возможно, избыточно для внутренней базы. |
| **Стоимость владения и использования** | Низкая: бесплатно после GPU, нет API-расходов; ongoing — только электроэнергия (~$10-50/мес для high-volume) и обслуживание/поддержка. | Pay-per-use: $0.02-0.13/1M токенов (small/large). Для 10k документов/мес: $1-10; растет с объемом. |

**Вывод**:  
Для 18k+ документов и ежемесячного обновления (~400 docs), **локальные эмбеддинги — оптимальны**: дешево, быстро, безопасно.

## Векторные СУБД

| Критерий | ChromaDB | FAISS |
| --- | --- | --- |
| **Скорость индексации** | Средняя: 500–3000 векторов/сек (на CPU+GPU) | Очень высокая: 5 000–50 000+ векторов/сек (особенно на GPU) |
| **Скорость поиска (k=10)** | 1–15 мс при 100k–1M векторов (HNSW) | 0.3–5 мс при тех же объёмах (IVF + HNSW + GPU) |
| **Сложность внедрения** | Очень низкая: 5–10 строк кода, встроенный сервер | Средняя–высокая: нужно писать обёртку, управлять индексами вручную |
| **Удобство в работе** | автосохранение, коллекции, метаданные, встроенный HTTP/API, LangChain/LlamaIndex «из коробки» | чистая библиотека, нет persistence, нет API, нет фильтрации по метаданным без доп.усилий |
| **Фильтрация по метаданным** | Нативная, очень удобная | Только вручную (post-filtering) или через внешние обёртки |
| **Масштабируемость** | До ~10–20M векторов на одной ноде без проблем | До сотен миллионов и миллиардов, но нужно самому шардировать |
| **Стоимость владения** | бесплатно, затраты инженера при масштабировании | бесплатно, затраты инженера на разработку и поддержку |

**Вывод**:  
Несмотря на более высокую скорость, **FAISS неудобен для динамической базы знаний** с ежемесячным приростом 400+ документов. **ChromaDB лучше подходит** для QuantumForge: он поддерживает фильтрацию по типу документа (например, «только Confluence» или «только SCADA-интеграции») и упрощает аудит.

## Рекомендуемая серверная конфигурация

| Параметр | Минимальная | Оптимальная (запас на 2–3 года) |
| --- | --- | --- |
| **CPU** | 16 ядер / 32 потока (Ryzen 9 7950X или Xeon Silver 4410Y) | 24–32 ядра / 48–64 потока (Ryzen 9 7950X3D или 2× Xeon Gold 5418Y) |
| **RAM** | 128 GB DDR5 | 192–256 GB DDR5 |
| **GPU (LLM-инференс)** | 1 × RTX 4090 24 GB (или 1 × A6000 48 GB) | 2 × RTX 4090 24 GB или 1 × RTX 5090 32 GB или 1 × H100 80 GB |
| **GPU (эмбеддинги)** | Тот же GPU | Тот же GPU (или отдельный RTX 4060 Ti 16 GB для переиндексации) |
| **Storage** | 2 TB NVMe Gen4/Gen5 (например, Samsung 990 PRO) | 4 TB NVMe Gen5 (RAID0 из двух дисков для скорости) |
| **Комментарий** | Работает, но при пике >80 пользователей начинает расти latency до 1.2–1.8 сек | Комфортный запас по latency даже при 150+ пользователях и одновременной переиндексации |

## Сравнение архитектурных вариантов

| Параметр | Вариант A: Локальный стек | Вариант B: Гибридный | Вариант C: Полностью облачный |
| --- | --- | --- | --- |
| **LLM** | Mistral 7B, Llama 3 8B или Qwen3 8b (локально) | Локальный для конфид. + GPT-3.5 для общих тем | GPT-4 Turbo |
| **Эмбеддинги** | `BAAI/bge-base-en-v1.5` (локально) | То же | `text-embedding-3-small` |
| **Векторная БД** | ChromaDB | ChromaDB | Pinecone / Weaviate |
| **Инфраструктура** | Own server или AWS EC2 (g5.xlarge) | AWS EKS + On-prem ChromaDB | Fully managed cloud (OpenAI + Pinecone) |
| **Конфиденциальность** | Полный контроль | Частичная (облако — только для публичных тем) | Риск утечки |
| **Стоимость (мес.)** | ~$0 (после CapEx) | ~$600–$900 (облако + хостинг) | ~$2000+ |
| **Масштабируемость** | Требует DevOps | Высокая | Максимальная |
| **SOC 2 совместимость** | Полностью | Требует строгой сегрегации запросов | Не проходит аудит |

## Итоговая рекомендация

**Выбираем Вариант A (локальный стек)**.

**Обоснование**:

*   QuantumForge работает с чувствительными данными промышленных клиентов, что делает локальное хранение и обработку обязательным.
*   ChromaDB обеспечивает гибкость обновления и фильтрацию по источникам — критично при разрозненной документации.
*   Локальные эмбеддинги и LLM соответствуют требованиям SOC 2 и исключают риски передачи данных.
*   Хотя первоначальные затраты выше, решение должно окупиться за счёт экономии FTE и сокращения 140 часов на аудит.
*   Архитектура масштабируема

# Task 2. Подготовка базы знаний

Выбрана [вики](https://house.fandom.com/ru/wiki/%D0%97%D0%B0%D0%B3%D0%BB%D0%B0%D0%B2%D0%BD%D0%B0%D1%8F_%D1%81%D1%82%D1%80%D0%B0%D0%BD%D0%B8%D1%86%D0%B0) по сериалу Доктор Хаус.

Скрипт [prepare\_kb.py](scripts/prepare_kb.py):

*   скачивает статьи о персонажах и эпизодах
*   получает список актеров и персонажей, динамически формирует подменные имена для них
*   выполняет замену в исходных статьях с учетом падежей и частей имен

# Task 3. Создание векторного индекса базы знаний



# Task 4. Реализация RAG-бота с техниками промптинга

# Task 5. Запуск и демонстрация работы бота